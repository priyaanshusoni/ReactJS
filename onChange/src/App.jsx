import { useState } from 'react'
import MyComponent from './MyComponent' 

function App() {
  
  return (
    <>
    <MyComponent/>
    
    </>
  )
}

export default App

/* `onchange()` is a function that is typically used in HTML and JavaScript to handle changes in input
fields, such as text inputs, checkboxes, and select dropdowns. When a user interacts with an input
field and its value changes, the `onchange()` function is triggered, allowing you to perform actions
or update the state based on the new value of the input field. In the provided code snippet,
`onchange()` is mentioned but not implemented, so its specific functionality would depend on how it
is defined and used within the application. */
