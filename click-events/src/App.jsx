
import './App.css'
import Button from './Button'
import Image from './Image'
function App() {
 

  return (
    <>
     <Button/>
     <Image/>
    </>
  )
}

export default App

/* The `click event in react` is a common event handler used in React applications to respond to user
clicks on elements. When a user clicks on a specific element, such as a button or a link, the click
event is triggered, and you can define a function to handle that event. This allows you to perform
actions or update the UI based on the user's interaction with the application. */
// Basically an interaction when  user clicks on a specific element We can respond to clicks by passing a callback to the Onclick event handler 